/// Layout style for the live list view.
enum LiveListViewStyle {
  /// Single column, full-screen paging mode with blur background and preview.
  singleColumn,

  /// Double column grid mode (default).
  doubleColumn,
}
